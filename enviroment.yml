name: cnnClassifier  # You can change "myenv" to the desired environment name
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8  # You can adjust the Python version as needed
  - tensorflow
  - pandas
  - dvc
  - notebook
  - numpy
  - matplotlib
  - seaborn
  - python-box
  - pyyaml
  - tqdm
  - ensure==1.0.2
  - joblib
  - types-pyyaml
  - scipy
  - flask
  - flask-cors
  - pip
  - pip:
    - -e .
